// TODO: interface chuẩn cho LLM provider (generate script/text).
// Impl thật (OpenAI/Anthropic/...) thêm sau ở providers/llm/impl/, có config qua ai_provider/ai_model.
export interface LlmProvider {}
